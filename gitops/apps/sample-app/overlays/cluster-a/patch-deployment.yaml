apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-app
  namespace: sample-app
spec:
  template:
    spec:
      containers:
        - name: app
          env:
            - name: CLUSTER_NAME
              value: "cluster-a"
